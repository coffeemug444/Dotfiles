#!/usr/bin/env bash
set -e
set -u
set -o pipefail

if [ ! $# -eq 1 ]; then
   echo "wrong usage"
   exit 1
fi

if [ "$1" == "vim" ]; then
   okular "$HOME/.local/vimcheat.md" &
else
   curl cheat.sh/$1 | less
fi
